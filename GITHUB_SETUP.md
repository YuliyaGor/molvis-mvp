# 🚀 Підключення до GitHub

## Крок 1: Створіть новий репозиторій на GitHub

1. Перейдіть на https://github.com/new
2. Введіть назву репозиторію: `molvis-mvp` (або іншу назву)
3. **НЕ** ініціалізуйте з README, .gitignore або ліцензією (у нас вже є локальний git)
4. Натисніть "Create repository"

## Крок 2: Прив'яжіть локальний репозиторій до GitHub

Після створення репозиторію, GitHub покаже вам команди. Використайте ці:

### Якщо ваш username на GitHub: `your-username`
### Якщо назва репозиторію: `molvis-mvp`

```bash
# Додайте remote origin (замініть your-username на ваш GitHub username)
git remote add origin https://github.com/your-username/molvis-mvp.git

# Або якщо ви використовуєте SSH:
git remote add origin git@github.com:your-username/molvis-mvp.git

# Перевірте, що remote додано
git remote -v

# Відправте код на GitHub
git push -u origin main
```

## Крок 3: Перевірте

Оновіть сторінку вашого репозиторію на GitHub - ви побачите весь код!

## 🔐 Важливо: Environment Variables

**НЕ ЗАБУДЬТЕ**: файл `.env.local` не буде завантажено на GitHub (він в .gitignore).

Коли будете деплоїти проект, вам потрібно додати ці змінні оточення:

### На Vercel/Netlify:
```
GEMINI_API_KEY=your_gemini_api_key
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Локально для інших розробників:
Створіть файл `.env.local` та додайте ці змінні.

## 📝 Наступні коміти

Після зміни коду:

```bash
# Перевірте зміни
git status

# Додайте файли
git add .

# Створіть коміт
git commit -m "Опис змін"

# Відправте на GitHub
git push
```

## 🌿 Робота з гілками (опціонально)

```bash
# Створіть нову гілку для фічі
git checkout -b feature/new-feature

# Внесіть зміни, зробіть коміт
git add .
git commit -m "Add new feature"

# Відправте гілку на GitHub
git push -u origin feature/new-feature

# Створіть Pull Request на GitHub
```

## ✅ Готово!

Тепер ваш код на GitHub і доступний для:
- Спільної розробки
- Deployment (Vercel, Netlify, тощо)
- Backup
- Відстеження змін
