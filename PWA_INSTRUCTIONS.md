# 📱 Встановлення MOLVIS як PWA

MOLVIS тепер підтримує Progressive Web App (PWA) - можна встановити додаток на телефон як нативний!

## ✅ Що готово:

1. ✅ PWA пакет встановлено (`@ducanh2912/next-pwa`)
2. ✅ Manifest.json створено з налаштуваннями:
   - Назва: "MOLVIS - Твій AI-SMM мольфар"
   - Коротка назва: "Molvis"
   - Колір теми: #000000 (чорний)
3. ✅ Іконки створено:
   - icon-192x192.png
   - icon-512x512.png
   - apple-touch-icon.png
4. ✅ Next.js конфігурація оновлена
5. ✅ Layout оновлено з metadata для PWA

## 📲 Як встановити на телефон:

### iPhone (Safari):
1. Відкрийте сайт у Safari
2. Натисніть кнопку "Поділитися" (квадрат зі стрілкою вгору)
3. Прокрутіть вниз і виберіть "На екран «Домівка»"
4. Натисніть "Додати"
5. Готово! Іконка MOLVIS з'явиться на домашньому екрані

### Android (Chrome):
1. Відкрийте сайт у Chrome
2. Натисніть меню (три крапки)
3. Виберіть "Додати на головний екран" або "Встановити додаток"
4. Підтвердіть встановлення
5. Готово! Іконка MOLVIS з'явиться на домашньому екрані

## 🚀 Переваги PWA:

- ⚡ Швидший запуск
- 📵 Працює офлайн (кешування сторінок)
- 📱 Повноекранний режим (як нативний додаток)
- 🔔 Підтримка push-сповіщень (можна додати пізніше)
- 💾 Менше використання пам'яті порівняно з браузером

## 🛠️ Для розробників:

### Запуск в dev mode:
```bash
npm run dev
```
PWA відключено в development режимі для зручності розробки.

### Build для production:
```bash
npm run build
npm start
```

### Генерація нових іконок:
```bash
node scripts/generate-icons.js
```

### Перевірка PWA:
1. Build проекту: `npm run build`
2. Запустіть production: `npm start`
3. Відкрийте Chrome DevTools → Application → Manifest
4. Перевірте Service Worker → Application → Service Workers

## 📝 Конфігурація:

### next.config.ts
```typescript
const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
});
```

### manifest.json
Файл знаходиться в `public/manifest.json`

## 🎨 Іконки:

- **192x192px** - для Android (home screen)
- **512x512px** - для Android (splash screen)
- **apple-touch-icon.png** - для iOS (home screen)

Всі іконки мають градієнт purple→pink з літерою "M".

## 🔧 Troubleshooting:

### Іконка не відображається на телефоні:
- Перевірте, чи правильно зібрано production build
- Очистіть кеш браузера
- Видаліть старий PWA і встановіть знову

### Service Worker не реєструється:
- Переконайтеся, що сайт працює по HTTPS (обов'язково для PWA)
- Перевірте консоль браузера на помилки

### PWA не пропонує встановлення:
- Переконайтеся, що manifest.json доступний
- Перевірте, що всі іконки існують в public/
- PWA критерії встановлення можуть відрізнятися в різних браузерах
